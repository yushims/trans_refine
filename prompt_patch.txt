DETERMINISTIC PATCH MODE (multilingual, ASR-aware)

CRITICAL OUTPUT FORMAT:
Return exactly ONE JSON object (no markdown/prose) with EXACT keys:
tokenization, translation, aggressiveness_level, speaker_scope, ct_speaker, ct_combine, no_touch_tokens, ct_lexical, ct_disfluency, ct_format, ct_numeral, ct_punct, ct_casing.

Goal: single-run patch using the ordered chain below, with minimal edits.
Tie-breaks: fewest edits → fewest chars changed → fewest punctuation insertions → earliest position.

Allowed edits only: typos, spacing/formatting, duplicate/disfluency cleanup, minimal local grammar fixes, punctuation for run-ons/closing pairs.
Forbidden: rewrites/paraphrases/reordering/style/idiom edits, semantic-content deletion, unsupported lexical additions (hallucinations).
Prefer to avoid em dash; use language-appropriate punctuation alternatives.
Prefer not to perform lexical substitution inside protected entity/legal-name spans unless directly supported by explicit in-line evidence; otherwise keep the current step input unchanged.

PHASE OVERVIEW:
0) Global constraints and output contract (always active).
1) Preprocessing (executed once, outside chain): TOKENIZATION, ENGLISH_TRANSLATION, AGGRESSIVENESS_LEVEL, SPEAKER_SCOPE.
2) Chain execution (step-by-step): apply only steps listed in CHAIN, in order.

PHASE 1 - PREPROCESSING (outside chain, executed once):

[TOKENIZATION]
   - Tokenize ORIGINAL once. No normalization; tokens are exact ORIGINAL substrings.
   - Use hybrid segmentation in this order:
     a) Protected-span longest-match first: URL/email, number-like/currency/percent, dates/times, web handles/IDs.
     b) For whitespace-delimited scripts (e.g., Latin/Cyrillic/Arabic with spaces): whitespace-separated word tokens; keep punctuation/symbols as separate tokens.
     c) For non-whitespace-delimited scripts: Han/Kana per character; Hangul as chunked runs.
     d) Any remaining punctuation/symbols are standalone tokens.
   - Whitespace is a separator, never a token.

[ENGLISH_TRANSLATION]
  - Produce one preprocessing artifact: a faithful English translation of the ORIGINAL input sentence.
  - Keep names, numbers, units, URLs/emails/handles, and other protected spans unchanged when copying into the translation context.
  - This translation is a reference artifact only; it must not, by itself, trigger edits, rewrites, or step skipping.
  - Do NOT add any new output keys: keep strict output schema unchanged.

[AGGRESSIVENESS_LEVEL]
   - Estimate aggressiveness_level once for transcript cleanup intensity: low, medium, or high.
   - Base this level on transcript quality signals (ASR-like disfluencies, punctuation sparsity, boundary ambiguity, token noise).
   - Lock this value for the rest of the chain; do not change it later.
   - Use aggressiveness mapping:
     a) low: conservative (minimal punctuation + minimal fluency cleanup).
     b) medium: balanced.
     c) high: aggressive for punctuation + fluency cleanup only.
   - Regardless of mode: never paraphrase/rewrite and never violate Forbidden constraints.

[SPEAKER_SCOPE]
  - Detect whether the ORIGINAL segment is single-speaker or multi-speaker.
  - Output `speaker_scope` as exactly one of: `single`, `multi`, `unknown`.
  - Use `multi` only when there is explicit evidence of turn-taking/dialogue with distinct speaker turns.
  - Use `single` when no such evidence exists.
  - Use `unknown` only when evidence is insufficient/ambiguous.

Global cross-step policy (applies during PHASE 2 only): for every ACTIVE step in the chain, treat `translation` as a soft reference anchor only for ambiguity resolution of English word form, numeral formatting preference, and letter casing. Use this anchor only to support minimal, local edits that remain within the current step scope and all Forbidden constraints. Do not use `translation` to trigger rewrites, semantic substitutions, step skipping, or cross-step violations.

PHASE 2 - CHAIN EXECUTION:

STEP_LIBRARY (chain steps only; reference text. Do NOT execute steps not in CHAIN):

[SPEAKER]
Purpose: Add/normalize speaker labels while preserving original wording.
Rules:
- If plain-text speaker labels already exist, normalize them into bracketed tag form regardless of `speaker_scope`.
- Canonical speaker label format is bracketed tag + trailing colon: `[LABEL]:`.
- If `speaker_scope` is not `multi` and no speaker labels exist, pass through unchanged.
- If `speaker_scope` is `multi` and no labels exist, add minimal turn labels in order: `[SPK1]:`, `[SPK2]:`, ...
- For plain-text labels, normalize to bracketed tags (for example `Speaker 1:` -> `[SPK1]:`, `Abc:` -> `[ABC]:` when mapping is unambiguous).
- Do not rewrite lexical content, punctuation, numerals, casing, or spacing except what is strictly required to attach/normalize speaker tags.
- Preserve turn order and boundaries exactly; do not merge/split turns.

[COMBINE]
Purpose: Combine separated token fragments for identifiers/products/proper-names/entities, numbers, thousands/decimal separators, currency/finance, dates/times, web/handles, multipliers, and other tightly bound expressions when the split is meaningless (format-only).
Rules:
- COMBINE is join-only for split fragments; do not perform value normalization or semantic conversion in this step.
- Allowed: spacing/boundary fixes that only merge two or more consecutive split parts.
- Compact unambiguous numeric suffixes that are purely split by spaces.
- Forbidden: year expansion or numeral-style conversion; defer these to NUMERAL.

[NO_TOUCH]
Purpose: Extract exact spans/tokens from the COMBINE result for proper names and legal-entity names; lock them as immutable protected tokens for all remaining steps.
Rules:
- Extract as exact substrings from the COMBINE result (no normalization).
- Protected spans must not be lexically edited in later steps (no character replacement, script-variant replacement, insertion, deletion, or reorder inside span).

[LEXICAL]
Purpose: Fix lexical errors only (typos and ASR lexical substitutions).
Rules:
- Preserve pronunciation similarity and meaning; if uncertain, keep the original token.
- For English token choices or mixed-script English terms, prefer forms supported by `translation` when evidence is ambiguous.
- Do NOT edit any protected token/span from NO_TOUCH (no replace/insert/delete/reorder inside protected span).
- For protected entity/legal-name spans: keep current step input unchanged unless correction is directly supported by explicit in-line evidence.

[DISFLUENCY]
Purpose: Fix fluency/grammar only (disfluencies and minimal required grammar) while preserving meaning.
Rules:
- For no-space languages, add words only when omission is unmistakable and the shortest fix.
- Do not apply numeral/format normalization or token recombination in this step.
- Apply aggressiveness from AGGRESSIVENESS_LEVEL only to disfluency cleanup intensity.
- Do NOT delete long repeated spans; keep them to avoid audio-text alignment drift.
- Do NOT remove repeated content that belongs to different speakers/turns.
- Do NOT edit any protected token/span from NO_TOUCH (no replace/insert/delete/reorder inside protected span).

[FORMAT]
Purpose: Fix formatting only.
Rules:
- Normalize only residual format-only spacing inside tightly bound symbolic/abbreviated expressions; do not recombine lexical tokens already handled by COMBINE.
- For English abbreviations/compact forms, prefer format patterns aligned with `translation` when unambiguous.
- Keep fixed compounds and marker forms compact when spacing changes meaning only in format (not content).
- Keep lexicalized/proper-name compounds in standard compact form (do not split joined forms like brand/product names).
- Treat organization/entity legal names as protected spans: only format-only edits (spacing/punctuation/casing/script style) are allowed; if lexical form is uncertain, keep current step input unchanged.

[NUMERAL]
Purpose: Fix numeral formatting/normalization only.
Rules:
- Normalize unambiguous numeral expressions (including numeral-local spacing/boundary presentation such as percent/currency/version/number+unit forms) to canonical written form across languages, only when format-only (not a meaning rewrite), especially for years/dates, ordinals/cardinals, percentages, currency amounts, versions, and IDs.
- For numeral style decisions with multiple valid forms, prefer the form that matches `translation` when this does not alter meaning.
- Language-conventional style: normalize numeral expressions to the customary written style of the current language/register, and keep style consistent within the same sentence/segment.
- Multilingual multiplicative forms: normalize unambiguous mixed digit+multiplier forms to canonical digits while preserving unit/script conventions.
- Changes must preserve pronunciation/readout intent; if pronunciation would change or is uncertain, keep original wording.
- For unambiguous spoken percentage expressions, convert to numeric percent form using `%`.
- Year policy (ASR-safe): keep 2-digit year forms unchanged by default. Expand 2-digit years to 4-digit years only with strong explicit disambiguation (e.g., nearby explicit century anchor or clearly anchored range).
- For already explicit full-year expressions, convert to digits when unambiguous.
- If numeral intent is ambiguous, keep original wording.

[PUNCT]
Purpose: Fix punctuation/run-ons.
Rules:
- Use language-appropriate sentence-end marks; prefer neutral declarative punctuation.
- Use locked aggressiveness_level from AGGRESSIVENESS_LEVEL to choose punctuation aggressiveness (conservative/balanced/aggressive), while still following clear boundaries/clauses.
- When punctuation is needed to prevent run-ons, decide left-to-right and insert sentence-ending punctuation at the earliest clear boundary; do not delay to later ambiguous points.
- Add missing sentence-ending punctuation before sentence-start capitalization.
- For character-based languages, replace every inter-character spaces with the correct punctuation, or remove the spaces if that’s better.
# - Prefer Clause-Linking: Downgrade sentence-ending marks to commas unless there is a significant shift in topic or the current thought is explicitly finalized; avoid "staccato" short sentences.
- Do NOT insert punctuation adjacent to existing punctuation if it would create redundant/stacked marks.

[CASING]
Purpose: Fix wrongly cased tokens.
Rules:
- Capitalize sentence starts that are currently lowercased after sentence-ending punctuation.
- Without a sentence boundary, do NOT introduce mid-sentence capitalization except for proper nouns/entities.
- For proper nouns/entities, follow their conventional writing style or use `translation` as a casing reference when casing is ambiguous.
- Do NOT add/remove/move punctuation in this step; only change letter case.

CHAIN ORDER (within PHASE 2, execute in this exact order):
{chain_steps}
- ACTIVE = steps listed in CHAIN; INACTIVE = all other steps in STEP_LIBRARY.
- Execute only ACTIVE steps.
- INACTIVE steps must be pass-through:
  {"edits": [], "result": <unchanged input-to-that-step>} (or [] for no_touch_tokens).
- Each step reads the current text and produces its stage output; the step result becomes the next step input.
- Execution/completion rule: for each ACTIVE step, process the full text left-to-right and emit the step result only after exhaustively fixing all in-scope cases (or explicitly leaving ambiguous cases unchanged).
- Execution discipline addendum: every ACTIVE step must consult `translation` as a soft reference for English word form, numeral formatting preference, and casing disambiguation.
- Edits disambiguation: interpret each [before, after] left-to-right on the first unmatched occurrence in the current step input.
- Each step may perform only its own edit type; if an edit belongs (or could belong) to another step, leave it unchanged in the current step and defer it to the designated step.

Output fields (always present):
- ct_speaker = {"edits": [[before, after], ...], "result": text after SPEAKER}
- ct_combine = {"edits": [[before, after], ...], "result": text after COMBINE}
- no_touch_tokens = extracted in NO_TOUCH as unique exact substrings in first-appearance order (or [] if NO_TOUCH inactive)
- ct_lexical = {"edits": [[before, after], ...], "result": text after LEXICAL}
- ct_disfluency = {"edits": [[before, after], ...], "result": text after DISFLUENCY}
- ct_format = {"edits": [[before, after], ...], "result": text after FORMAT}
- ct_numeral = {"edits": [[before, after], ...], "result": text after NUMERAL}
- ct_punct = {"edits": [[before, after], ...], "result": text after PUNCT}
- ct_casing = {"edits": [[before, after], ...], "result": text after CASING}
- Use {"edits": [], "result": <unchanged>} when no edits were made.

Determinism target: prefer idempotent output on re-run, no meaningful segment loss, no hallucinated lexical content, and consistent punctuation/capitalization/spacing.

Schema:
- tokenization: {"tokens":[...ORIGINAL tokens from preprocessing...]}
- translation: English translation string from ENGLISH_TRANSLATION preprocessing
- aggressiveness_level: one of "low", "medium", "high"
- speaker_scope: one of "single", "multi", "unknown"
- ct_*: {"edits": [[before, after], ...], "result": string}
- no_touch_tokens: ["token/span1", "token/span2", ...]
- no additional keys

Input transcript:
{input_transcript}
