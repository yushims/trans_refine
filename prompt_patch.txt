DETERMINISTIC PATCH MODE (multilingual, ASR-aware)

Output exactly ONE JSON object (no markdown/prose) with EXACT keys:
tokenization, machine_transcription_probability, ct_combine, no_touch_tokens, ct_lexical, ct_disfluency, ct_format, ct_numeral, ct_punct, ct_casing.

Goal: single-run patch using the ordered chain below, with minimal edits.
Tie-breaks: fewest edits → fewest chars changed → fewest punctuation insertions → earliest position.

Preprocessing (outside chain):
Tokenization: tokenize ORIGINAL once. No normalization; tokens are exact ORIGINAL substrings.
Use hybrid segmentation in this order:
1) Protected-span longest-match first: URL/email, number-like/currency/percent, dates/times, web handles/IDs.
2) For whitespace-delimited scripts (e.g., Latin/Cyrillic/Arabic with spaces): whitespace-separated word tokens; keep punctuation/symbols as separate tokens.
3) For non-whitespace-delimited scripts: Han/Kana per character; Hangul as chunked runs.
4) Any remaining punctuation/symbols are standalone tokens.
Whitespace is a separator, never a token.

Allowed edits only: typos, spacing/formatting, duplicate/disfluency cleanup, minimal local grammar fixes, punctuation for run-ons/closing pairs.
Forbidden: rewrites/paraphrases/reordering/style/idiom edits, semantic-content deletion, unsupported lexical additions (hallucinations).
Prefer to avoid em dash; use language-appropriate punctuation alternatives.
Prefer not to perform lexical substitution inside protected entity/legal-name spans unless directly supported by explicit in-line evidence; otherwise keep the current step input unchanged.

Chain order (apply in order):
0) First estimate machine_transcription_probability from transcript quality signals (ASR-like disfluencies, punctuation sparsity, boundary ambiguity, token noise). Lock this value for the rest of the chain; do not change it later.
    - <= 0.3: conservative mode (minimal punctuation and minimal fluency cleanup).
    - > 0.3 and <= 0.8: balanced mode.
    - > 0.8: aggressive mode for punctuation and fluency cleanup only.
    - Regardless of mode: never paraphrase/rewrite, never violate Forbidden constraints.
1) Combine separated token fragments for identifiers/products/proper-names/entities, numbers, thousands/decimal separators, currency/finance, dates/times, web/handles, multipliers, and other tightly bound expressions when the split is meaningless.
2) Extract exact spans/tokens from the step 1 result for proper names and legal-entity names, and lock them as immutable protected tokens for all remaining steps.
3) Then fix lexical errors only (typos and ASR lexical substitutions):
    - Preserve pronunciation similarity and meaning; if uncertain, keep the original token.
    - Do NOT edit any protected token/span extracted in step 2 (no character replacement, script-variant replacement, insertion, deletion, or reorder inside the protected span); for protected entity/legal-name spans, keep the current step input unchanged unless a correction is directly supported by explicit in-line evidence.
4) Then fix fluency/grammar only (duplicates/disfluencies and minimal required grammar) while preserving meaning:
    - For no-space languages, add words only when omission is unmistakable and the shortest fix.
    - Do not apply numeral/format normalization or token recombination in this step.
    - Apply aggressiveness from step 0 only to disfluency cleanup intensity.
5) Then fix formatting only:
    - Normalize only residual format-only spacing inside tightly bound symbolic/abbreviated expressions; do not recombine lexical tokens already handled by step 1.
    - Keep fixed compounds and numeric-marker forms compact when spacing changes meaning only in format (not content).
    - Keep lexicalized/proper-name compounds in standard compact form (do not split joined forms like brand/product names).
    - Treat organization/entity legal names as protected spans: only format-only edits (spacing/punctuation/casing/script style) are allowed; if lexical form is uncertain, keep the current step input unchanged.
6) Then fix numeral formatting/normalization only:
    - Normalize unambiguous spoken numeral expressions to canonical written numerals across languages when this is format-only (not a meaning rewrite), especially for years/dates, ordinals/cardinals, percentages, currency amounts, versions, and IDs.
    - Number formatting changes must preserve pronunciation/readout intent in the source language; if pronunciation would change or is uncertain, keep the original wording.
    - For unambiguous spoken percentage expressions, convert to numeric percent form using `%`.
    - For year-like expressions with explicit year markers (language-specific), prefer digits when unambiguous.
    - If numeral intent is ambiguous, keep the original wording.
7) Then fix punctuation/run-ons:
    - Use language-appropriate sentence-end marks; prefer neutral declarative punctuation.
    - Use machine_transcription_probability from step 0 to choose punctuation aggressiveness (conservative/balanced/aggressive), while still following textual boundaries/clauses.
    - When punctuation is needed to prevent run-ons, decide left-to-right and insert sentence-ending punctuation at the earliest clear clause/sentence boundary; do not delay to later ambiguous points.
    - Do NOT change/add/remove punctuation at the end of the line.
    - Do NOT insert punctuation adjacent to existing punctuation if it would create redundant/stacked marks.
8) Then fix capitalization only:
    - Sentence-ending punctuation requires following sentence-start capitalization.
    - Sentence-start capitalization requires preceding sentence-ending punctuation.
    - Without a sentence boundary, do NOT introduce mid-sentence capitalization except for proper nouns/entities.
    - For proper nouns/entities, follow their conventional writing style.
    - Do NOT change the casing of the first token of the line.

Output each text-edit stage explicitly in these required fields:
- ct_combine = {"edits": [[before, after], ...], "result": text after chain step 1}
- no_touch_tokens = ["token/span1", "token/span2", ...] extracted in chain step 2 from step 1 result (exact substrings)
- ct_lexical = {"edits": [[before, after], ...], "result": text after chain step 3}
- ct_disfluency = {"edits": [[before, after], ...], "result": text after chain step 4}
- ct_format = {"edits": [[before, after], ...], "result": text after chain step 5}
- ct_numeral = {"edits": [[before, after], ...], "result": text after chain step 6}
- ct_punct = {"edits": [[before, after], ...], "result": text after chain step 7}
- ct_casing = {"edits": [[before, after], ...], "result": text after chain step 8}
- For each step, use an empty list when no edits were made: {"edits": [], "result": <unchanged text>}.

Determinism target: prefer idempotent output on re-run, no meaningful segment loss, no hallucinated lexical content, and consistent punctuation/capitalization/spacing.

Schema:
- tokenization: {"tokens":[...ORIGINAL tokens from preprocessing...]}
- machine_transcription_probability: number in [0,1]
- ct_combine: {"edits": [[before, after], ...], "result": string}
- no_touch_tokens: ["token/span1", "token/span2", ...]
- ct_lexical: {"edits": [[before, after], ...], "result": string}
- ct_disfluency: {"edits": [[before, after], ...], "result": string}
- ct_format: {"edits": [[before, after], ...], "result": string}
- ct_numeral: {"edits": [[before, after], ...], "result": string}
- ct_punct: {"edits": [[before, after], ...], "result": string}
- ct_casing: {"edits": [[before, after], ...], "result": string}
- no additional keys

Input transcript: